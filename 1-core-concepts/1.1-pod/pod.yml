apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
    tier: frontend
spec:
  containers:
    - name: nginx
      image: nginx
# minukube start --driver docker
# kubectl run nginx --image=nginx
# kubectl apply -f 01-pod/pod.yml 
# kubectl get pods
# kubectl describe pod nginx
# kubectl delete pod nginx
# kubectl delete all --all